import React from 'react'
import { Header } from '../Header/Header'
import { Contain } from '../Contain/Contain'

export const Home = () => {
  return (
    <div>
        <Header/>
        <Contain/>
    </div>
  )
}
